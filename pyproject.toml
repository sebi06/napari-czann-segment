[build-system]
requires = ["setuptools>=61.0.0", "wheel", "setuptools_scm"]
build-backend = "setuptools.build_meta"


[tool.setuptools_scm]
write_to = "src/napari_czann_segment/_version.py"

[tool.black]
line-length = 79

[tool.isort]
profile = "black"
line_length = 79
